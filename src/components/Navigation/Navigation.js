import React from "react";
import styles from "./Navigation.module.scss";

const Navigation = props => {
  return (
    <React.Fragment>
      <img className={`${styles.navigation}`} />
    </React.Fragment>
  );
};

export default Navigation;
